#include <iostream>
#include <cmath>
using namespace std;

int main () {
  double x = 1.000;
double y = 1.000;
  int z = 0;
while (y > x) {
  if (fmod(x,y) != 0 || x = 1) {
y = y + 1;
  z = z + 1;
  }
else {
cout << "the number " << x << " is not a prime number";
  }
}
  if (z = x - 1 && y = x - 1) {
    y = 1;
      z = 0;
    while (y < (exp2(x))-1) {
    if (fmod((exp2(x))-1, y != 0 || y = 1) {
      y = y + 1;
      z = z + 1;
    }
      else {
      x = x + 1;
    }
  }
}
