#include <iostream>
#include <cmath>
using namespace std;

int main () { 
double x = 1.000;
double y = 1.000;
  int z = 0;
int a = 0;
double b = 1.000;
while (y > x) {
  if (fmod(x,y) != 0 || x = 1) {
y = y + 1;
  z = z + 1;
  }
else {
x = x + 1;
  }
}
  if (z = x - 1 && y = x - 1) {
    while (y < (exp2(x))-1) {
    if (fmod((exp2(x))-1, y != 0 || y = 1) {
      b = b + 1;
      a = a + 1;
    }
      else {
      x = x + 1;
    }
      if (a = b = (exp2(x)) - 1 && z = y = x - 1) {
        cout << exp2(x-1)*(exp2(x)) - 1
      }
  }
}
